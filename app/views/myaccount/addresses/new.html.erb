<div class='nine columns'>
  <h3> Novo endereço </h3>

  <%= form_for(@address, :url => myaccount_addresses_path(@address)) do |f| %>
    <%= render :partial => 'form', :locals => {:f => f} %>
  <% end %>
  <div class=''>
    <%= link_to 'Voltar', myaccount_addresses_path, :class => 'secondary small button  ' %>
  </div>
</div>
