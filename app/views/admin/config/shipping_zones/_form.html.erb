<% if @shipping_zone.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@shipping_zone.errors.count, "erro não permitiu", "erros não permitiram") %> que essa área de envio fosse salva:</h2>

    <ul>
    <% @shipping_zone.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>

<fieldset>
  <label>Nome</label>
  <%= f.text_field :name %>
</fieldset>
<div class="actions">
  <%= f.submit "Criar", :class => "button" %>
</div>
