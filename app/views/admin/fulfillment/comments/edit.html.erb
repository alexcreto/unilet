<h1> Editar Comentário </h1>

<%= form_for(@comment, :url => admin_fulfillment_order_comment_path(order, @comment)) do |f| %>
  <%= render :partial => 'form', :locals => {:f => f} %>
<% end %>

  <%= link_to 'Mostrar', admin_fulfillment_order_comment_path(order, @comment ), :class => 'button' %> |
  <%= link_to 'Mostrar todos', admin_fulfillment_order_comments_path(order), :class => 'button' %>
